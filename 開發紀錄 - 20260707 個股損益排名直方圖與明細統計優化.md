# 顧股咕 - 股票記錄平台開發紀錄 (2026-07-07)

本開發紀錄詳細記載了 2026 年 7 月 7 日所完成的「個股損益績效排名直方圖（垂直柱狀圖）」功能實作，以及「歷史交易明細統計摘要」在手機端的響應式版面呈現與反應式過濾計算優化。

---

## 📋 本次版本優化目標
1. **個股損益績效排名直方圖（垂直柱狀圖）：** 於持股庫存頁面新增「個股損益績效排名」圖表，可透過 Tabs 一鍵切換「未實現損益」與「已實現損益」排名。
2. **X軸股票/Y軸金額與正負中心對稱：** X軸（橫向）顯示股票代號與名稱，Y軸（縱向）顯示損益金額。以正中央（50% 高度）為 0 軸線。正值（獲利）向上增長，負值（虧損）向下延伸。
3. **優化圖表色彩與視覺對比：** 正值採用暖色調紅橘漸層（`from-rose-600 to-rose-455`）；負值採用冷色調綠色漸層（`from-emerald-400 to-emerald-600`）。背景加入 25%、50%（0 軸實線與 "0" 數值標章）、75% 三條水平網格線，解決前版配色吃力與錯位問題。
4. **手機端防擠壓與字體放大：** 圖表支援平滑橫向滾動（`overflow-x-auto no-scrollbar`），防止大量股票時欄位被壓縮。數值標籤（金額、代號、名稱、0 軸標籤）字體全面放大，並具備智慧縮寫格式化（例如 `+1.2萬` 或 `-3.5k`），確保在有限寬度下極佳的清晰度。
5. **交易明細統計摘要手機版呈現：** 在歷史交易明細頁面之手機版版面（md 以下）新增一個 2x2 緊湊網格統計摘要卡片，讓手機用戶也能直觀檢視累計股息、折讓、已實現損益與紀錄筆數。
6. **統計摘要過濾重新計算（含 lazy evaluation bug 修復）：** 修正 Vue 3 的 computed 懶計算特性導致 transactions 在明細頁面未被 enrichment 的 bug。現在無論是用戶篩選個股、關鍵字或交易類型，統計摘要數值都會跟著過濾條件實時動態重新計算。

---

## 🛠️ 技術實作細節與核心程式碼

### 1. 💾 交易資料結構擴充 (`stock.ts`)
* **實作路徑：** `types/stock.ts`
* **變更重點：**
  * 在 `Transaction` 介面中新增可選屬性 `realized_gain?: number` 與 `dividend_amount?: number`，以供 TypeScript 編譯階段支援附帶於交易物件上的損益數值。

---

### 2. 🧮 歷史交易依序 Enrichment 損益計算 (`usePortfolio.ts`)
* **實作路徑：** `app/composables/usePortfolio.ts` 與 `composables/usePortfolio.ts`
* **設計原理：**
  * 修改核心 `calculatePositions` 函數。在對 `transactions` 進行時間軸移動平均成本計算時，依序對每一筆交易進行標註。
  * 遇到 `SELL`（賣出）交易時，計算該筆賣出相對於當時平均成本之已實現損益，並將其寫入 `tx.realized_gain`。
  * 遇到 `DIVIDEND`（配息）交易時，計算該筆配息金額（股數 * 每股配息 - 匯費），寫入 `tx.dividend_amount`。
  * 其他交易類型預設重置為 `0`。

---

### 3. 📊 個股損益績效垂直柱狀圖 (`portfolio.vue`)
* **實作路徑：** `app/pages/portfolio.vue`
* **變更重點：**
  * **垂直柱狀圖對稱定位：** 輔助線容器定位在 `top-6 h-44`（對齊柱狀圖高度），將 `top-[50%]` 定為中心 0 軸。正向柱體利用 `bottom: 50%; height: getBarHeightPercentage(val)%` 向上生長；負向柱體利用 `top: 50%; height: getBarHeightPercentage(val)%` 向下生長。
  * **智慧縮寫與字體：** 新增 `formatChartValue` 將損益金額以「萬」或「k」單位縮寫。金額標籤字體升級至 `text-[11px]`、股票代號 `text-xs`、股票名稱 `text-[10px]`、0 軸標籤 `text-[10px]`。
  * **防擠壓滾動與隱藏滾動條：** 圖表 Column 寬度設為 `w-14`（56px），間距設為 `gap-2`。包裹於 `overflow-x-auto no-scrollbar` 中，並於 scoped style 中新增 Chrome/Firefox 的隱藏滾動條樣式。

---

### 4. 📜 明細頁反應式統計與懶載入修復 (`history.vue`)
* **實作路徑：** `app/pages/history.vue`
* **變更重點：**
  * **Vue 懶載入 Bug 修復：** 在 computed `sortedTimeline` 開頭加入 `const _ = portfolio.allPositions.value;`。這強迫 Vue 觸發 composable 內 position 的計算，確保交易物件在進入明細頁面時已經附帶了 `realized_gain` 等 enrichment 屬性。
  * **反應式統計：** 新增 `filteredSummary` 計算屬性，依據 `filteredTimeline` 中被篩選過的明細，重算已領股息、折讓、已實現損益以及筆數。
  * **手機版摘要版型：** 新增 `lg:hidden` 區域，以 `grid grid-cols-2 gap-3` 將 `filteredSummary` 呈現在搜尋篩選欄位正下方，提供手機用戶一流的視覺體驗。

---

## 📊 Git 版本提交
* **Commit 訊息：** `feat: implement vertical column stock ranking chart and optimize reactive history statistics for mobile`
* **分支：** `main`
