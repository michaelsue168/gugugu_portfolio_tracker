# 顧股咕 - 股票記錄平台開發紀錄 (2026-05-27)

本開發紀錄詳細記載了 2026 年 5 月 27 日所完成的功能優化、系統排版修正、API 升級以及全新交易紀錄編輯功能之技術實作細節。

---

## 📋 本次版本優化目標
1. **現價機制實時化：** 淘汰模擬隨機價格，引進 Yahoo Finance 真實股市報價，並實作失敗安全回退與明確警示標籤。
2. **iOS 滾輪式日期選取器 (Wheels Date Picker)：** 實作流暢的原生質感滾輪組件，取代不便於手機操作的傳統月曆。
3. **新增表單重疊與預設值修正：** 將手續費預設折讓設為無折扣，並隱藏買賣模式下的重複手續費輸入框，防止版面重疊。
4. **流水帳明細編輯功能 (Bottom Sheet Modal)：** 提供抽屜式編輯對話框，供用戶隨時修正歷史交易紀錄並自動即時重算。

---

## 🛠️ 技術實作細節與核心程式碼

### 1. 🎡 iOS 滾輪式日期選取器 (`WheelDatePicker.vue`)
* **實作路徑：** `app/components/WheelDatePicker.vue`
* **設計原理：** 
  * 採用 **CSS Scroll Snap** 機制實作年、月、日三個獨立的垂直滾動列，以獲得 60fps 的原生手機物理滾動回彈感。
  * 透過 `scroll-snap-type: y mandatory` 設定滾動容器，並於子選項套用 `scroll-snap-align: center`，確保滾動結束時數字能完美鎖定在中央高亮區。
  * 實作 **點擊直接選取與平滑滑動 (Click to Center)**：點擊任意數字時，自動計算該項目的 `scrollTop` 偏置量，調用 `scrollColumnTo(el, index)` 觸發 `scrollTop` 變更與平滑移位，帶來極致操作感。
  * **立體 3D 淡出特效：** 在選取器上下邊界覆蓋 `absolute bg-gradient-to-b / to-t from-slate-950` 的半透明遮罩，使未選中的外圍數字呈 3D 圓柱體般的弧形漸變透明。
  * **閏年與月份聯動：** 計算屬性 `days` 根據當前選定的 `year` 與 `month` 動態取得該月份的正確天數（自動解決大月 31 天、小月 30 天、閏二月 29 天及平二月 28 天的邏輯判定）。

---

### 2. 📈 Yahoo Finance 真實現價 API 整合與安全回退
* **實作路徑：** 
  * 後端：`server/api/refresh-prices.ts`
  * 狀態計算：`app/composables/usePortfolio.ts`
* **真實現價抓取：**
  * 在後端定義非同步 API `fetchYahooStockPrice(code)`。
  * 優先嘗試上市代號後綴 `${code}.TW`（例如 `2330.TW`），若抓取失敗或回傳為空，自動 fallback 嘗試上櫃代號 `${code}.TWO`（例如 `8069.TWO`）。
* **失敗回退與警示設計：**
  * 若因代碼不存在（如測試代號）或斷網導致 API 獲取失敗，後端將該股票的現價儲存為 `0.00` 作為**失敗哨兵值**。
  * 前端 Composable 進行 Position 計算時，若偵測到現價 `rawPrice <= 0`，則自動設定：
    * `pos.current_price = pos.average_cost`（使未實現損益歸零，避免顯示離譜的偏誤數據）。
    * `pos.price_fetch_failed = true`。
  * **明確警示 UI：** 於持股庫存頁面 (`portfolio.vue`) 與首頁焦點持股 (`index.vue`) 清單上，若 `price_fetch_failed` 為真，自動在價格與 ROI 旁渲染黃色的 `⚠️ 獲取失敗 (暫以成本計算)` 警告標籤。

---

### 3. ✍️ 流水帳交易紀錄編輯器 (Bottom Sheet Modal)
* **實作路徑：** `app/pages/history.vue`
* **介面設計：** 
  * 於每筆流水帳右側垃圾桶按鈕旁，新增一個 `✏️ 編輯（Pencil）` 按鈕。
  * 點選時觸發 `openEditModal(item)` 將該項目資料深拷貝（Clone）至反應式 Ref `editingItem`。
  * 手機端為底部滑出抽屜式對話框，桌面端則自適應為置中發光卡片，具備毛玻璃遮罩 (`backdrop-blur-sm bg-slate-950/75`)。
* **資料安全防呆：**
  * 股票代號、名稱與交易動作設為**唯讀鎖定**，不可變更，以防修改代碼導致底層移動平均成本計算邏輯錯亂。
  * 可編輯交易日期（完美串接 `WheelDatePicker`）、單價、股數（一律為正數輸入，存檔時會根據買/賣動作自動適配正負號符號）、手續費淨額、證交稅等。
* **Supabase 自動重算聯動：**
  * 用戶點擊「儲存修改」後，發送 `update` API 更新對應的 `transactions` 或 `cash_flows` 資料表。
  * 更新成功後自動調用 `portfolio.fetchData()`，系統將根據最新交易歷史自動重新計算移動平均成本、總投入與資產市值。

---

### 4. 🛠️ 表單細節排版與預設折扣修正
* **實作路徑：** `app/pages/record.vue`
* **變更重點：**
  * 將預設折扣 `brokerDiscount` 改為 `1.0` (無折扣/10折)。
  * 將提交按鈕由「寫入資料庫」改為更簡短直觀的 **「儲存」**。
  * **重疊欄位修正：** 原本 `BUY` 與 `SELL` 模式下，手續費已在「券商手續費預估」卡片中計算並綁定 `form.fee`，底部的「手續費(其他)」亦會同步出現造成排版重疊。現已修改判斷，底部欄位**僅在「配息」或「配股」時顯示**。
  * **佈局自適應：** 當手續費欄位隱藏時，「交易日期」滾輪組件將自動橫跨兩列 (`col-span-2`) 滿版，畫面完美對稱。

---

## 📊 Git 版本提交記錄
* **Commit 雜湊碼：** `bbef339`
* **Commit 訊息：** `feat: upgrade to Yahoo Finance API, add WheelDatePicker & bottom sheet edit modal`
* **分支：** `main` -> `origin/main` (GitHub)
* **部署聯動：** 已推送至 GitHub，Vercel 將自動觸發 Webhook 完成生產環境的 Auto-Deployment。
