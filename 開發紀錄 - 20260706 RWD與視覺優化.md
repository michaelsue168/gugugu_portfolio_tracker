# 顧股咕 - 股票記錄平台開發紀錄 (2026-07-06)

本開發紀錄詳細記載了 2026 年 7 月 6 日所完成的響應式網頁設計 (RWD) 版面升級、多欄與網格佈局優化、大螢幕側邊欄選單實作，以及針對全域灰色小字的視覺辨識度與對比度調校之技術實作細節。

---

## 📋 本次版本優化目標
1. **全域 RWD 版型調校：** 在電腦與平板（螢幕寬度 $\ge$ 768px）等大螢幕裝置下，提供如同桌面端 App 般流暢的「左側固定導覽列（Sidebar） + 右側獨立內容滾動區」版面；在手機端（< 768px）則無縫保留「頂部標頭 + 底部導覽列 + 模擬外殼居中限制」的完美操作體驗。
2. **總覽首頁 7:5 響應式二欄化：** 大螢幕下由垂直單欄改為左右二欄佈局，左側呈現總市值卡片與資產比例圖例，右側呈現焦点持股。
3. **持股庫存網格化：** 庫存列表改為 2 至 3 欄響應式網格，並設定 card items 對齊方式防止因個別卡片展開而造成相鄰卡片被縱向拉伸。
4. **交易表單限寬居中：** 限制流水帳新增表單之最大寬度（`max-w-2xl`），確保在大螢幕兩側有合適留白，避免輸入欄位過長導致視覺分散。
5. **明細流水帳 8:4 分欄與側邊歷史統計：** 流水帳明細在大螢幕改為二欄，左側為明細列表，右側新增一個反應式「歷史統計摘要」卡片，隨時呈現已領股息、累計折讓、已實現損益等數據。
6. **灰色小字視覺辨識度優化：** 全面調大並調亮所有微小的說明文字、表單 Label 與日期標註（例如由 `text-[9px] text-slate-500` 放大調亮至 `text-xs text-slate-400` 或 `text-[11px] text-slate-350`），消除在暗色背景下對比度不足、字體過小的閱讀疲勞。

---

## 🛠️ 技術實作細節與核心程式碼

### 1. 🖥️ 全域響應式側邊欄與多載版型外殼 (`default.vue`)
* **實作路徑：** `app/layouts/default.vue`
* **設計原理：** 
  * 透過 Tailwind 的 `md:flex` 媒體查詢條件判定，在電腦/平板端啟用左側 `aside` 側邊欄（內含漸層「咕」Logo、`navItems` 導覽清單、以及底部會員帳號狀態和安全登出按鈕）。
  * 內容主要顯示區設定為 `md:h-screen md:overflow-hidden`，並將 `<main>` 設為 `overflow-y-auto w-full max-w-7xl mx-auto md:p-6 lg:p-8`。如此一來，在大螢幕下頁面內容會在大容器中單獨滾動，保留如 Native App 般的質感。
  * 手機端時，側邊欄會完全隱藏（`hidden md:flex`），自動將行動端專屬的 `header`（頂部標頭）與 `nav`（固定於底部的精緻浮動導覽列）渲染出來。
  * **未登入狀態防干擾**：在用戶未登入時（如 `/login` 頁），外層容器會變更為 `max-w-md mx-auto justify-center`。如此一來，登入面板不論在何種裝置寬度下，均能完美居中且維持緊湊小巧。

---

### 2. 📊 總覽首頁的 RWD 多欄配置 (`index.vue`)
* **實作路徑：** `app/pages/index.vue`
* **變更重點：**
  * 將原本 `space-y-6` 的單一垂直縱列包覆於 `grid grid-cols-1 lg:grid-cols-12 gap-6 items-start`。
  * 左側（`lg:col-span-7`）容納「總資產發光卡片」與「資產配置比例圖例」，大幅改善橫向拉伸問題。
  * 右側（`lg:col-span-5`）容納「持股庫存焦點」卡片，並在大螢幕下將原先截斷展示的 3 檔股票延伸顯示至 5 檔，優化資訊密度。

---

### 3. 🕸️ 庫存清單的多欄自適應網格 (`portfolio.vue`)
* **實作路徑：** `app/pages/portfolio.vue`
* **變更重點：**
  * 引入 `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4 items-start` 容器裝載個股卡片。
  * **關鍵設計 `items-start`**：在 CSS Grid 佈局中，若無設定 `items-start`，當某個個股卡片被點擊展開（展示買進、賣出、配息流水帳）時，同一個 Grid Row 的其他卡片會因為 Flex/Grid 預設拉伸（Stretch）而跟著變高，破壞版面。使用 `items-start` 可令同列卡片維持原先的自然高度，排版更加精美。

---

### 4. 📝 集中防拉伸表單 (`record.vue`)
* **實作路徑：** `app/pages/record.vue`
* **變更重點：**
  * 於最外層加入 `max-w-2xl mx-auto` 控制表單最大寬度。
  * 提高內距至 `md:p-8` 並進行欄位左右對齊，使得在桌機和平板上填寫買進賣出等交易資訊時更具包覆感。

---

### 5. 📜 流水帳明細分欄與反應式統計卡片 (`history.vue`)
* **實作路徑：** `app/pages/history.vue`
* **變更重點：**
  * **過濾選單扁平化**：利用 `flex flex-col md:flex-row md:items-center md:justify-between gap-4`，使搜尋列與類別 pills 在大螢幕下並排，不再佔用過多縱向高度。
  * **統計卡片側邊欄**：在 `lg:col-span-4` 區域內新增桌面端限定的「歷史統計摘要」卡片。
  * 該卡片直接讀取 `usePortfolio` 內共享的 `portfolio.summary.value` 響應式屬性，即時呈現累計股息、累計折讓及已實現損益，不需發起額外的 DB API Request。

---

### 6. 👁️ 灰色小字視覺可讀性優化 (全域)
* **優化細節：**
  * 解決深色主題（`bg-slate-950`）下小字看不清的問題。
  * 舉凡 description 文字、交易日期的 `font-mono` 小字、手續費與證交稅說明、各輸入框上方的 `label` 標題、以及統計摘要內的標示，全面調升一個 font-size 等級（例如：`text-[9px]` 升至 `text-[11px]` 或 `text-xs`），並將灰階顏色代碼由偏暗的 `text-slate-500` 全面升級為亮灰色的 `text-slate-450`、`text-slate-400`、或 `text-slate-300`，使對比度符合 W3C 無障礙網頁色彩對比規範。

---

## 📊 Git 版本提交預告
* **Commit 訊息：** `style: implement RWD layout with desktop sidebar, responsive grid, and optimize typography contrast`
* **分支：** `main`
