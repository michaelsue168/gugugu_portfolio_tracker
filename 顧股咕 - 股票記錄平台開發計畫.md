# 顧股咕 - 股票記錄平台開發計畫 (GuGuGu Portfolio Tracker)

## 專案概述

本專案旨在打造一個專為手機端優化的個人股票交易紀錄網頁系統（PWA），解決傳統 Excel 記帳在庫存分批買進（複數交易）時難以計算移動平均成本、未實現損益、賣出損益，以及難以整合獨立折讓收入與股息的痛點。

## 技術棧定義 (Tech Stack)

* **前端框架：** Nuxt 3 (SSR/SSG, 開發全端路由)
* **樣式框架：** Tailwind CSS (行動端優先，Responsive Design)
* **程式語言：** TypeScript (嚴格型態定義)
* **動畫庫：** GSAP (GreenSock Animation Platform，用於數字滾動與 UI 順暢切換)
* **後端與資料庫：** Supabase (Database, Auth, Client-side CRUD)
* **部署平台：** Vercel

## 程式碼撰寫守則
1. **Nuxt 3 規範**：
   - 優先使用 Composition API `<script setup lang="ts">`。
   - 嚴禁手動 import Nuxt 內建的 composables (如 ref, computed, useFetch)，利用其自動導入機制。
   - 敏感操作或 Supabase 密鑰管理必須寫在 `server/api/` 中。
2. **GSAP 規範**：
   - GSAP 動畫必須封裝在 `onMounted()` 或 `watchEffect()`（且確保 `process.client` 為真）中，避免 SSR 階段引發 window/document 錯誤。
   - 組件銷毀時（`onUnmounted`），必須呼叫 `gsap.context()` 的 `revert()` 以防止記憶體洩漏。
3. **Supabase 與 TypeScript**：
   - 資料庫互動必須使用嚴格的 TypeScript 型態定義。
   - 優先使用 Supabase 組合式函數，並正確處理 `error` 狀態與 Loading 結構
   
---

## 資料庫架構 (Supabase Schema)

請在 Supabase 中建立以下三張資料表，並開啟 RLS (Row Level Security) 確保用戶只能讀寫自己的資料。

```sql
-- 1. 股票交易明細表 (歷史流水帳)
CREATE TABLE transactions (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  stock_code VARCHAR(10) NOT NULL,          -- 股票代號 (如: 2330)
  stock_name VARCHAR(50) NOT NULL,          -- 股票名稱 (如: 台積電)
  action VARCHAR(20) NOT NULL,              -- 'BUY', 'SELL', 'DIVIDEND', 'BONUS_SHARES'
  price DECIMAL(12, 2) DEFAULT 0.00,        -- 交易單價
  shares INT DEFAULT 0,                     -- 股數 (買進為正，賣出為負)
  fee INT DEFAULT 0,                        -- 手續費淨額
  tax INT DEFAULT 0,                        -- 證交稅 (賣出才有)
  transaction_date DATE NOT NULL,           -- 交易日期
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 2. 獨立折讓與現金流表 (與特定股票無關之收入)
CREATE TABLE cash_flows (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  type VARCHAR(20) NOT NULL,                -- 'DISCOUNT' (折讓/退佣), 'OTHER'
  amount INT NOT NULL,                      -- 金額
  note TEXT,                                -- 備註
  record_date DATE NOT NULL,                -- 紀錄日期
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 3. 股票現價快取表 (定時與手動更新)
CREATE TABLE stock_prices (
  stock_code VARCHAR(10) PRIMARY KEY,
  stock_name VARCHAR(50) NOT NULL,
  current_price DECIMAL(12, 2) NOT NULL,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

```

---

## 核心商業邏輯與演算法

### 1. 移動平均成本法 (方案 A)

當用戶有多筆複數交易時，庫存狀態需依據交易時間由舊到新串接計算：

* **BUY (買進)：** 
$$新剩餘股數 = 舊剩餘股數 + 買進股數$$


$$新總投入成本 = 舊總投入成本 + (買進單價 \times 買進股數 + 手續費)$$


$$移動平均成本 = \frac{新總投入成本}{新剩餘股數}$$


* **SELL (賣出)：**

$$新剩餘股數 = 舊剩餘股數 - 賣出股數$$


$$新總投入成本 = 新剩餘股數 \times 舊移動平均成本$$



*(移動平均成本保持不變。賣出時之已實現損益 = $(\text{賣出單價} \times \text{賣出股數} - \text{手續費} - \text{證交稅}) - (\text{舊移動平均成本} \times \text{賣出股數})$ )*
* **BONUS_SHARES (配股/股票股利)：**

$$新剩餘股數 = 舊剩餘股數 + 配股股數$$


$$總投入成本不變，移動平均成本被稀釋 = \frac{舊總投入成本}{新剩餘股數}$$



### 2. 損益計算公式

* **目前市值** = $\text{目前剩餘股數} \times \text{當前現價}$
* **帳面未實現損益** = $\text{目前市值} - \text{目前總投入成本}$
* **未實現報酬率 (ROI)** = $(\frac{\text{帳面未實現損益}}{\text{目前總投入成本}}) \times 100\%$

---

## 實作任務拆解 (Todo List for AI Agent)

請 AI Agent 依據以下步驟循序漸進開發：

### Phase 1: 專案初始化與環境設定

1. [ ] 初始化 Nuxt 3 專案，設定 TypeScript 環境。
2. [ ] 安裝並設定 Tailwind CSS 與 `@nuxtjs/supabase`。
3. [ ] 安裝 `gsap` 及其 Nuxt 相關插件。
4. [ ] 在 `types/stock.ts` 中建立對應 SQL 的 TypeScript Interface（`Transaction`, `CashFlow`, `Position`）。
5. [ ] 設定 Supabase Auth，建立基礎的登入/登出頁面（支援 Google 或 Magic Link 登入）。

### Phase 2: 核心運算邏輯與 Composable 開發

1. [ ] 建立 `composables/usePortfolio.ts`。
2. [ ] 實作核心函數 `calculatePositions(transactions, priceMap)`，以**移動平均成本法**將流水帳聚合為即時庫存資料（需過濾掉已清倉股數為 0 的股票）。
3. [ ] 實作總資產計算函數，整合「未實現損益」、「歷史已實現損益」、「獨立折讓收入表 (`cash_flows`)」與「股票現金股利 (`DIVIDEND`)」。

### Phase 3: 行動端優化 UI 設計 (Tailwind CSS)

1. [ ] **Layout 設計：** 建立適合手機的底部導覽列 (Bottom Navigation Bar)：「總覽」、「資產庫存」、「交易流水帳」、「新增紀錄」。
2. [ ] **Dashboard 頁面：** * 頂部設計大字卡顯示總資產、總成本、總損益（配合 GSAP 數位滾動動畫與正負號紅綠顏色切換）。
* 設計圓餅圖或資產比例條。


3. [ ] **庫存列表頁面：** * 卡片式設計，顯示個股：庫存股數、平均成本、現價、未實現損益與 ROI。
* 右上角加入「🔄 刷新現價」按鈕，點擊時觸發 GSAP 旋轉動畫。


4. [ ] **防呆 CRUD 表單頁面：**
* 可切換「買進/賣出/股息/折讓」之 Segmented Control。
* 欄位切換時使用 GSAP 進行流暢的表單高度與淡入淡出過場。
* 選取買賣時，自動根據輸入的股價與股數，預估計算台灣證券手續費（含預設折讓折扣），並允許手動修改。



### Phase 4: API 與資料串接

1. [ ] 串接 Supabase Client 實作表單的新增 (Create)、讀取 (Read)、更新 (Update)、刪除 (Delete) 功能。
2. [ ] 在 Nuxt Server 建立 `/api/refresh-prices` 路由，用於手動與定時更新 `stock_prices` 表的現價（可先用 Mock 資料或串接 FinMind/Fugle 免費 API）。

### Phase 5: PWA 支援與部署準備

1. [ ] 設定 `@vite-pwa/nuxt`，設定網頁 manifest 與圖示，使其可在手機端「新增至主畫面」作為 PWA 運行。
2. [ ] 匯出環境變數設定，準備部署至 Vercel。

---

## 開發守則 (For AI Agent)

* **行動端優先：** 所有的排版與互動必須以手機操作（觸控、大按鈕、防呆輸入）為核心考量，拒絕密密麻麻的桌面端表格。
* **動畫效能：** 動畫請一律使用 GSAP 實作，確保在 iOS/Android 瀏覽器上的流暢度達到 60fps。
* **型態安全：** 嚴格禁止使用 `any`，所有計算與 API 撈取回來的資料都必須經過 TypeScript 型態驗證。

---

*現在，請讀取此文件並從 **Phase 1** 開始執行，完成後向我回報。*